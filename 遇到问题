qc:
    1.文件夹路径一定要写对 "/mibi/...."
    2. fastq 文件一定要是压缩的

assembly:
    最后生成的而文件夹有 finished

circularized:
    recycler: 问题是不能重命名contig的header
    scapp不能用 可能与blast版本有关联

isolation:
    plasmidverfy后提取质粒基因组,id不一样


functional annotation 中会涉及文件为空(如果先得到蛋白质,后得到non_redundant_gene_set)


要做的事情:

 1.比较工具,人造已知数据

 2. 加入binning 在linear那

 3.预测宿主

 4.加快,在计算coverage的时候用pandas而不是读写文件